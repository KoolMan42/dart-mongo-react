name: server
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.14.4 <3.0.0'


dependencies:
  args: ^2.0.0
  dart_mongo_lite: ^0.1.1
  json_annotation: ^4.4.0
  shelf: ^1.1.0
  shelf_plus: ^1.2.1
  shelf_router: ^1.0.0
  uuid: ^3.0.5

dev_dependencies:
  http: ^0.13.0
  lints: ^1.0.0
  test_process: ^2.0.0
  json_serializable: ^6.0.0
  test: ^1.15.0
  build_runner: ^2.1.7

